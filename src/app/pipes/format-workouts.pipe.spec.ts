import { FormatWorkoutsPipe } from './format-workouts.pipe';

describe('FormatWorkoutsPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatWorkoutsPipe();
    expect(pipe).toBeTruthy();
  });
});
